# Superseded by `tasks/TRACKER.md`

This file tracked Gates 1–7 (TS-084…TS-126). It has been consolidated, along
with `backlog.md`, `phase15_tracker.md`, and `spec_audit_tracker.md`, into a
single tracker: **[`tasks/TRACKER.md`](TRACKER.md)**.

All rows and narrative — including every footnote — live there now, unchanged.
This stub exists only so old links and habits don't 404. Kept for git history;
no longer a source of truth. Do not add new rows here.
